services:
    db:
        image: mysql:5.7
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: wordpress
            MYSQL_USER: wordpress
            MYSQL_PASSWORD: wordpress
        volumes:
            - db_data:/var/lib/mysql
        healthcheck:
            test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
            interval: 10s
            timeout: 5s
            retries: 5

    wordpress:
        # Use WordPress 6.9 with PHP 8.2 to meet plugin minimum requirements
        image: wordpress:6.9-php8.2-apache
        depends_on:
            db:
                condition: service_healthy
        environment:
            WORDPRESS_DB_HOST: db:3306
            WORDPRESS_DB_USER: wordpress
            WORDPRESS_DB_PASSWORD: wordpress
            WORDPRESS_DB_NAME: wordpress
            WORDPRESS_CONFIG_EXTRA: |
                if ( isset( $$_SERVER['HTTP_HOST'] ) ) {
                    $$scheme = ( ! empty( $$_SERVER['HTTPS'] ) && 'off' !== $$_SERVER['HTTPS'] ) ? 'https' : 'http';
                    define( 'WP_HOME', $$scheme . '://' . $$_SERVER['HTTP_HOST'] );
                    define( 'WP_SITEURL', $$scheme . '://' . $$_SERVER['HTTP_HOST'] );
                }
        ports:
            - '8000:80'
        volumes:
            - ./:/var/www/html/wp-content/plugins/common-wp-blocks
        healthcheck:
            test: ['CMD-SHELL', 'curl -f http://localhost:8000 || exit 1']
            interval: 10s
            timeout: 5s
            retries: 10

    cli:
        # Use the official wordpress CLI image which is published as `wordpress:cli`.
        # Some specific distro/version tags may not exist in registries; `wordpress:cli` is reliable.
        image: wordpress:cli
        depends_on:
            - wordpress
        volumes:
            - ./:/var/www/html/wp-content/plugins/common-wp-blocks
        entrypoint: >
            sh -c "until curl -sS http://localhost:8000/ | grep -q 'WordPress' || true; do sleep 2; done; \
            wp core install --url=http://localhost:8000 --title=wp --admin_user=admin --admin_password=pass --admin_email=admin@example.com --skip-email --path=/var/www/html || true; \
            wp plugin activate common-wp-blocks --path=/var/www/html || true; sleep infinity"

    playwright:
        # Use the Playwright Docker image that matches the upgraded @playwright/test version.
        # Use the tag without a distro suffix; some tags omit the '-focal' suffix.
        image: mcr.microsoft.com/playwright:v1.57.0
        depends_on:
            - wordpress
        working_dir: /workspace
        volumes:
            - ./:/workspace
            - ./logs:/workspace/logs
        environment:
            - WP_BASE_URL=http://wordpress
        entrypoint: ['tail', '-f', '/dev/null']

volumes:
    db_data:
